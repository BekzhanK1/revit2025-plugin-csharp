# Prompt — task-01-shared-ui

Подготовь базу для редизайна UI:

1. Обнови `WindowLayoutHelper` для широких окон (900–960 px).
2. По желанию вынеси общие WPF-стили в ResourceDictionary.
3. Не меняй бизнес-логику экранов — только layout foundation.

Spec: `agents-external-memory/plugin-ui-redesign/SPEC.md`
